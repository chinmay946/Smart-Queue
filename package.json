{
  "name": "smart-queue-root",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "vercel-build": "npm run build",
    "start": "cd server && node index.js"
  },
  "dependencies": {
    "serverless-http": "^3.0.0"
  }
}
